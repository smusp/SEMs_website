\documentclass[tikz, border = 10pt]{standalone}
\input{../SEMstyles.tex}

\begin{document}
\begin{tikzpicture}

\def \gap {4}            % separation between Dependents
\def \groups {1/No strategy, 2/Discussion, 3/Exercise}

\foreach \i/\j in \groups {
   %% Dependent
   \tikzmath{\x = \gap * (\i - 1);}        % x-coord for Dependents
   \node [manifest] (y\i) at (\x, 0) {Life\\Satisfaction};

   %% Covariates
   \node [manifest] (pre\i) [below = 1.5cm of y\i] {pre$_C$};
   \path [regression] (pre\i) edge ["b"] (y\i);

   %% Residuals
   \node [residual2] (e\i) [below right = 4mm of y\i] {e};
   \path [regression] (e\i) edge (y\i.south east);

   %% Means
   \node [constant] (const\i) [above = 1.5cm of y\i] {1};
   \path [regression] (const\i) edge ["a$_\i$"] (y\i);

   %% Groups
   \node [group] (gp\i) [above = 0.5cm of const\i] {Group \i:\\\j};
}

%% Separators
\foreach \i in {1,2} {
   \tikzmath{\j = int(\i + 1);}
   \coordinate (M) at ($(y\i) !0.5! (y\j)$);
   \draw [densely dashed] ($(M) !4.2cm!270: (y\i)$) -- ($(M) !3cm!90: (y\i)$);
}

\end{tikzpicture}
\end{document}