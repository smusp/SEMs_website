\documentclass[tikz, border = 10pt]{standalone}
\renewcommand{\familydefault}{\sfdefault}

\usetikzlibrary{positioning, calc, quotes, arrows.meta, shapes}
\usepackage{pagecolor}\pagecolor{red!20}

\tikzset{
  every edge quotes/.style = {fill = white},
  regression/.style = {-{Stealth[length = 1.5mm]}, shorten > = 1pt, inner sep = 1.5pt}
}

\begin{document}
\begin{tikzpicture}

%% Manifest
\node (x) [draw] {X};
\node (y) [right = 5cm of x, draw] {Y};
\node (m) [above = 4cm of $(x) !0.5! (y)$, draw] {M};

%% Regressions
\path [regression] (x)     edge ["c$'$"] (y)
                   (x.70)  edge ["a"] (m.250)
                   (m.290) edge ["b"] (y.110);

\end{tikzpicture}
\end{document}